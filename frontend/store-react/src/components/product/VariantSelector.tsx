import type {ProductVariant} from '../../types/catalog';
export function VariantSelector({variants,selected,onChange}:{variants:ProductVariant[];selected:ProductVariant;onChange:(v:ProductVariant)=>void}){return <fieldset className="variant-selector"><legend>Escolha a variante</legend>{variants.map(v=><button type="button" key={v.id} className={selected.id===v.id?'variant-option selected':'variant-option'} disabled={!v.available} aria-pressed={selected.id===v.id} onClick={()=>onChange(v)}><strong>{Object.values(v.attributes).join(' / ')}</strong><span>{v.available?'Disponível':'Indisponível'}</span></button>)}</fieldset>}
